name: Test
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Build the Docker image
      run: echo "Image pushed to ${{ secrets.REGISTRY_URL }}--${{ secrets.REGISTRY_USERNAME }}--${{ secrets.REGISTRY_PASSWORD }}"
